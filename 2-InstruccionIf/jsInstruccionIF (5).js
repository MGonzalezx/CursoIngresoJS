function mostrar()
{
//tomo la edad  
var edad;

edad = document.getElementById('edad').value ;

if(edad < 13 || edad > 17)
{
	alert("No es adolescente");
}

// or || si es esto o lo otro, and && entre esto y lo otro, not ! no es.
}//FIN DE LA FUNCIÓN